# Result provenance

Fresh results are generated only after training and evaluation. They are saved
under `results/<version>/`.

`results/reference_report_results.csv` contains values transcribed from the
supplied report/PPT for comparison. Those values are not automatically claimed
as fresh results.

The project intentionally does not manufacture or randomize metrics.
